/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Index from './src/router/Index.router';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Index);
